services:
  postgres:
    image: postgres:16-alpine
    container_name: cubo_ai_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cubo_ai
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
